<h1>Patients</h1>
<%= link_to "Add Patient", new_patient_path %>
<hr>

<h3><%= @patients.length <= 0 ? "No Patients Available" : "" %></h3>
<ul>
<% @patients.each do |patient| %>
  <li><%= link_to patient.full_name, patient_path(patient) %></li>
<% end %>
</ul>